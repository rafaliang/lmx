for $a in doc("./src/a.xml")//actor,
	$aa in $a/age,
	$an in $a/name,
	$s in doc("./src/a.xml")//singer,
	$sa in $s/age,
	$sn in $s/name
where $aa eq $sa
return
<people-with-ages>{
	$aa,
	<actor>{$a/name}</actor>,
	<singer>{$s/name}</singer>}
</people-with-ages>